import { Animal, Dog } from './09-protected';

// const animal = new Animal('elite'); // definir espesificamente el tipo de animal
// animal.greeting();

const cheis = new Dog('cheis', 'nico');
cheis.greeting();
cheis.woof(2);
